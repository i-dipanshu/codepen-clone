const question_no = "A2Q3";

const question = ``;

const html = `<p>Kripuri shripuri</p>`;

const css = `p{
  color: white;
}`;

const js = ``;

const Data = {
  question_no,
  question,
  html,
  css,
  js,
};

export default Data;
