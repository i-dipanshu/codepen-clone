const question_no = "A1Q1";

const question = `Create a page to show a HTML table and add CSS to make it look nicer. Add background color to alternate row in the table.`;

const html = `<p>Hello here!</p>
<p>Hello there!</p>
`;

const css = `p{
  color: red;
}
`;

const js = `function(){
  console.log("Hello there!);
}

`;

const Data = {
  question_no,
  question,
  html,
  css,
  js,
};

export default Data;
